# Streamlit Cloud: paste into Settings > Secrets (server side only).
# Local alternative to .env: copy to .streamlit/secrets.toml.
OPENAI_API_KEY = ""
OPENAI_MODEL = "gpt-5.5"
OPENAI_REASONING_EFFORT = "none"
